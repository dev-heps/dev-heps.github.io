# Project Specific Rules

- **Environment**: This project is developed and run using Docker. Always remember to use Docker-related commands or workflows (e.g., `docker-compose`) if environment setup, deployment, or complex dependencies are involved. 
